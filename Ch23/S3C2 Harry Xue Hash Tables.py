"""
S3C2 Opt3 Harry Xue Hash Tables
"""
class Node:
    def _init_(self):
        self.Data = Data
class Hash:
    def _init_(self):
        self.NewNode = NewNode

        for i in range(10):
            NewNode = Node()
            self.append.NewNode

def Insert(NewVal):
    i = Hash(NewVal.key)
    while HashTable[i]! = null:
        i = i+1
        if i>Max:
            i = 1
    HashTable[i] = NewVal

def FindRecord(SearchKey):
    i  =  Hash(SearchKey)
    while HashTable[i].key! = SearchKey and HashTable[i]! = null:
        i = i+1
        
        if i>Max:
            i = 0
    if HashTable[i]! = null:
        return HashTable[i]

def Hash(key):
    Address = key%(n+1)
    return Address

    
